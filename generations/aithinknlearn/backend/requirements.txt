# Multi-Agent Orchestration Dependencies
crewai
langchain
langchain-openai
langchain-community
python-dotenv
pydantic
fastapi
uvicorn
pytest
pytest-asyncio
httpx
openai

# RAG System Dependencies
chromadb>=1.0.0
numpy
# sentence-transformers requires PyTorch (~1.5GB) which OOMs Railway
# ChromaDB uses its built-in ONNX embeddings as fallback

# Phonetic Verification Dependencies
# Note: For production, add NVIDIA NeMo for G2P-Conformer model
# nemo_toolkit[asr]
# cmudict (CMU Pronouncing Dictionary)
# For now, using built-in dictionaries and rule-based conversion

# PDF Generation Dependencies
reportlab
Pillow
